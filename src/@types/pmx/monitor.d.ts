export function enableProbes(custom_namespace: any): any;
export class enableProbes {
	constructor(custom_namespace: any);
	interval: number;
}
declare function stopProbing(): void;
export {
	enableProbes as enableProbe,
	stopProbing as stopProbe,
	stopProbing as stopProbes,
};
